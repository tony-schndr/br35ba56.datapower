---
- name: Read local cert file
  slurp:
    src: /DEV/{{ item }}.crt
  register: local_files
  loop: "{{ ca_names }}"

- name: Upload cert to default domain
    debug:
      msg: "inner item = {{item.content}}"
      #var2: item.1
      
    loop: "{{local_files.results}}"