- name: 'Demo apply_conf'
  hosts: dp1
  connection: local
  tasks:
    - name: Try to apply some config to a domain
      community.datapower.apply_config:
        state: present
        domain: default
        config: 
          CryptoValCred:
            name: valcred
            mAdminState: enabled
            Certificate:
            - value: Test2
            - value: Test1
            CertValidationMode: legacy
            UseCRL: 'off'
            RequireCRL: 'on'
            CRLDPHandling: ignore
            ExplicitPolicy: 'off'
            CheckDates: 'on'
    
